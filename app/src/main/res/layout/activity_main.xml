<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    android:textSize="18sp"
    android:textStyle="bold|italic"
    app:fontFamily="sans-serif">

    <include
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="250dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:addStatesFromChildren="true"
        android:animateLayoutChanges="true"
        android:animationCache="true"
        android:background="#FFFFFF"
        android:descendantFocusability="beforeDescendants"
        android:fitsSystemWindows="true"
        android:focusable="auto"
        android:focusableInTouchMode="true"
        android:hapticFeedbackEnabled="true"
        android:layerType="hardware"
        android:scrollbarThumbHorizontal="@color/design_default_color_secondary_variant"
        android:visibility="visible"
        app:headerLayout="@layout/nav_header_main"
        app:insetForeground="@color/design_default_color_secondary_variant"
        app:itemBackground="@color/design_default_color_background"
        app:itemIconTint="@color/design_default_color_secondary_variant"
        app:itemShapeFillColor="#3949AB"
        app:itemTextAppearance="@style/TextAppearance.AppCompat.Medium"
        app:itemTextColor="#008080"
        app:menu="@menu/activity_main_drawer"
        tools:visibility="visible" />

</androidx.drawerlayout.widget.DrawerLayout>
